{
  "name": "generic-design-system",
  "version": "0.1.0",
  "private": true,
  "description": "An open source design system that's fully customizable and agent ready",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "storybook": "pnpm --filter @gds/docs storybook",
    "cli": "pnpm --filter @gds/cli dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}